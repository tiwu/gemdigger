# Plan: Fix ESLint Dependency Issue After Dependency Update

## Overview
Due to a recent dependency update to ESLint `^10.8.0`, the linter requires `@eslint/js` to be explicitly added as a `devDependency` to successfully load configuration rules in `eslint.config.js`. This plan outlines the installation of `@eslint/js` at the root and server workspaces, verifying linting compliance, and archiving this plan to `./adr/`.

## Progress Tracking
- [x] Create plan.md file
- [x] Add @eslint/js devDependency to root package.json and run npm install
- [x] Add @eslint/js devDependency to server package.json and run npm install
- [x] Verify linting and build
- [x] Update README.md
- [x] Archive plan to adr/ folder

## Implementation Steps
1. **Create implementation plan**: Write this plan to `plan.md`. (Done)
2. **Install @eslint/js in root**: Add `@eslint/js` as a `devDependency` to the root `package.json` (`^10.8.0`). (Done)
3. **Install @eslint/js in server**: Add `@eslint/js` as a `devDependency` to `server/package.json` (`^10.8.0`). (Done)
4. **Verify linting**: Run `npm run lint` at the root to verify both ESLint and Stylelint complete without errors. (Done)
5. **Update README.md**: Add any notes if necessary, or ensure general documentation is kept in sync. (Done - no changes needed)
6. **Move plan to ADR**: Archive the plan to the `./adr` folder timestamped correctly. (Done)
